export enum STATUS {
  DEFAULT = 0,
  FETCHING = 1,
  SUCCESS = 2,
  FAILURE = 3,
}

export const BUTTON_HEIGHT = 80
export const BUTTON_RADIUS = BUTTON_HEIGHT / 2

export const TEXT_SIZE = 30
export const CROSS_SIZE = 65
export const SUCCESS_SIZE = 40
